export interface Cohort {
    id: number,
    programmeId: number, 

    cohortName: string,
    cohortNumber: number,

    students: string,
    cohortDescription: string,
    description: string,
    image: string,
    createdAt: string,
    startDate: string,
    endDate: string,
    status: string,
}